import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
    toolbar: {
        height: "96px"
    },
    logo: {
        height: "80px",
        width: "80px",
        position: "absolute",
        top: "0"
    },
    mobileIcon: {
        fontSize: "2rem",
        color: "#FFF"
    }
}));
